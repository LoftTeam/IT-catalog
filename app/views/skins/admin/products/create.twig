{% extends 'templates/template_admin.twig' %}
{%  block  content %}

    <h1>Добавить товар</h1>
    {% if result %}
        <p class="alert alert-success">{{ result }}</p>
    {% else %}
        {% if errors %}
            {% for error in errors %}
                <p class="alert alert-danger">{{ error }}</p>
            {% endfor %}
        {% endif  %}
        <form method="post">
            <div class="form-group">
                <label >Название товара</label>
                <input name="product_name" type="text" class="form-control" placeholder="Название товара">
            </div>
            <div class="form-group">
                <label >Картинка</label>
                <input name="product_img" type="text" class="form-control" placeholder="Пути к картинке">
            </div>
            <div class="form-group">
                <label >Бренд</label>
                <input name="mark" type="text" class="form-control" placeholder=Бренд>
            </div>
            <div class="form-group">
                <label >Кол-во</label>
                <input name="count" type="text" class="form-control" placeholder="Кол-во">
            </div>
            <div class="form-group">
                <label >Цена</label>
                <input name="price" type="text" class="form-control" placeholder="Цена">
            </div>
            <div class="form-group">
                <label >Описание</label>
                <textarea name="description" id="" cols="70" rows="10" placeholder="Описание"></textarea>
            </div>
            <div class="form-group">
                <label >Категория</label>
                <select class="form-control" name="category_id">
                    {% for category in categories %}
                        <option  value="{{ category.id }}">{{ category.title }}
                        </option>
                    {% endfor %}
                </select>
            </div>
            <button name="submit" type="submit" class="btn btn-success">Создать</button>
        </form>
    {% endif %}
{% endblock %}